<?php defined('ABSPATH') || die; ?>

<div class="glsr-review" id="{{ review_id }}">
    {{ rating }}
    {{ assigned_links }}
    {{ content }}
    {{ avatar }}
    {{ author }}
    {{ response }}
</div>
